// const scaleCanvas = (event, gridCanvas, cellCanvas, currentScale) => {
//   event.preventDefault();

//   let scale = currentScale + event.deltaY * -0.01;
//   scale = Math.min(Math.max(0.125, scale), 4);

//   gridCanvas.scale(scale, scale);
//   cellCanvas;
// };

// export default scaleCanvas;
// export { scaleCanvas };
